server:
  http_listen_port: 3200

distributor:
  receivers:
    zipkin:
      endpoint: 0.0.0.0:9411 # Default Zipkin port

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo/blocks
